import styled from 'styled-components';
import theme from '../config/theme';

const CellHeadingSubtitle = styled.p`
  color: ${theme.midLightGrey};
  font-size: 0.85rem;
  margin: 0;
  font-style: italic;
  text-align: left;
`;

export default CellHeadingSubtitle;
